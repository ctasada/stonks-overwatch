# Stocks Portfolio

## Start Developing

```shell
python3 -m venv venv
source venv/bin/activate
pip install Django
```

For Windows
```shell
.\venv\Scripts\activate.bat
```

Run the server
```shell
python manage.py runserver
```
